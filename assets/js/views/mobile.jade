!!! 5
html
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, user-scalable=no")
        link(rel='stylesheet', href='#{_url}assets/css/jquery.mobile.min.css')
        link(rel='stylesheet', href='#{_url}assets/css/controle.css')
        title ""
        script(src='#{_url}assets/js/jquery.min.js')
        script(src='#{_url}assets/js/jquery.mobile.min.js')
        script(src='#{_urlSocket}/socket.io/socket.io.js')
        script(src='#{_url}assets/js/util.js')
        script(src='#{_url}assets/js/constants.js')
        script(src='#{_url}assets/js/main.js')
        script(src='#{_url}assets/js/joystick.js')
        script(src='#{_url}assets/js/ga.js')
        script(type='text/javascript')
            $(document).bind('pageinit', function() {
                var socket = io.connect(URL_SOCKET);
                var id = $('body').attr('id');

                socket.emit('setId', id + 'mb');

                setLanguage('#{_language}');
                loadScreen('#{_joystick}');

            });

    body(id="#{_id}")
        header
            h1(class="header title")
        section
            input(type="text", name="name", id="name")
            input(type="range", name="move", id="move", value="50", min="0", max="100", data-show-value="true", data-popup-enabled="true")
            p Eu sei! Esse controle é escroto! É que ainda não consegui pensar em nada melhor! :)
